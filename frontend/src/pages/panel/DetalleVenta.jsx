import {useEffect,useState} from 'react';import {useParams} from 'react-router-dom';import {obtenerVenta} from '../../Services/ventas';
export default function DetalleVenta(){const {id}=useParams();const [v,setV]=useState(null);useEffect(()=>{obtenerVenta(id).then(setV)},[id]);if(!v)return <p className="p-8 text-white">Cargando...</p>;return <section className="p-8 text-white"><h1 className="text-3xl font-bold">Venta #{v.id}</h1><p className="mt-3">Subtotal: {v.subtotal} · Impuestos: {v.impuestos} · Total: {v.total}</p><ul className="mt-6 space-y-2">{v.detalles.map(d=><li key={d.id} className="rounded border border-gray-700 p-3">{d.nombre_item} · {d.cantidad} · {d.subtotal}</li>)}</ul></section>}
